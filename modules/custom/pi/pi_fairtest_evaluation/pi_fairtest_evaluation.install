<?php

// $Id: pi_fairtest_evaluation.install,v1.0 2009/11/30  ou_mbg Exp $

/**
 * Implementation of hook_install().
 */

function pi_fairtest_evaluation_install() {
// Create tables.
    drupal_install_schema('pi_fairtest_evaluation');
}

/**
 * Implementation of hook_uninstall().
 */

function pi_fairtest_evaluation_uninstall() {
// Remove tables.
    drupal_uninstall_schema('pi_fairtest_evaluation');
    variable_del('pi_fairtest_evaluation');
}

/**
 * Implementation of hook_schema().
 */

function pi_fairtest_evaluation_schema() {

    $schema['pi_fairtest_evaluation'] = array(
        'description' => t('PI fairtest_evaluation'),
        'fields' => array(
        'nid' => array(
        'description' => t('node id'),
        'type' => 'int',
        'unsigned' => TRUE,
        'not null' => TRUE,
        'size' => 'small',
        'default' => 0,
        ),
        'vid' => array(
        'description' => t('The current vid version identifier'),
        'type' => 'int',
        'unsigned' => TRUE,
        'not null' => TRUE,
        'size' => 'small',
        'default' => 0,
        ),
        'eval' => array(
        'description' => t('What do you think about the garden experiment?'),
        'type' => 'text',
        'size' => 'normal',
        'not null' => FALSE,
        ),
	
        ),
        'primary key' => array('vid'),
        'indexes' => array(
        'nid' => array('nid'),
        ),
    );

    return $schema;

}
